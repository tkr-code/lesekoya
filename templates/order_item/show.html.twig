{% extends 'base.html.twig' %}

{% block title %}OrderItem{% endblock %}

{% block body %}
    <h1>OrderItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_item.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ order_item.quantity }}</td>
            </tr>
            <tr>
                <th>Unit_price</th>
                <td>{{ order_item.unitPrice }}</td>
            </tr>
            <tr>
                <th>Units_total</th>
                <td>{{ order_item.unitsTotal }}</td>
            </tr>
            <tr>
                <th>Adjustments_total</th>
                <td>{{ order_item.adjustmentsTotal }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ order_item.total }}</td>
            </tr>
            <tr>
                <th>Produit_name</th>
                <td>{{ order_item.produitName }}</td>
            </tr>
            <tr>
                <th>Variant_name</th>
                <td>{{ order_item.variantName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('order_item_index') }}">back to list</a>

    <a href="{{ path('order_item_edit', {'id': order_item.id}) }}">edit</a>

    {{ include('order_item/_delete_form.html.twig') }}
{% endblock %}
