{% extends 'base.html.twig' %}

{% block title %}ShippingAmount{% endblock %}

{% block body %}
    <h1>ShippingAmount</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shipping_amount.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ shipping_amount.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('shipping_amount_index') }}">back to list</a>

    <a href="{{ path('shipping_amount_edit', {'id': shipping_amount.id}) }}">edit</a>

    {{ include('admin/shipping_amount/_delete_form.html.twig') }}
{% endblock %}
