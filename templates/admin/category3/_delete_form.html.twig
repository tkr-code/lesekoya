<form method="post" action="{{ path('app_admin_category3_delete', {'id': category3.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category3.id) }}">
    <button class="btn">Delete</button>
</form>
