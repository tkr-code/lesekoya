<!DOCTYPE html>

<title>Category3</title>

{% block body %}
    <h1>Category3</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category3.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ category3.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_category3_index') }}">back to list</a>

    <a href="{{ path('app_admin_category3_edit', {'id': category3.id}) }}">edit</a>

    {{ include('admin/category3/_delete_form.html.twig') }}
{% endblock %}
