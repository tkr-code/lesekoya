<form method="post" action="{{ path('article_option_delete', {'id': article_option.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article_option.id) }}">
    <button class="btn">Delete</button>
</form>
