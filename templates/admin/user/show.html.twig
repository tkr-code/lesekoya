{% extends 'admin/base.html.twig' %}
{% import "macros/link.html.twig" as links %}
{% import "macros/breadcrumb.html.twig" as utils %}
{% block title %}User
{% endblock %}

{% block breadcrumb %}
	{{ utils.breadcrumb('Afficher un utilisateur',[])}}
{% endblock %}

{% block body %}
	<div class="card card-outline card-primary">
		<div class="card-body">
			{% include "includes/user/user_show.html.twig" %}

			{% if app.request.get('_route') == 'user_index' %}
				{{ links.back(path('user_index'), 'back to user list') }}
			{% endif %}
            {% if app.request.get('_route') == 'user_show_client' %}
			    {{ links.back(path('admin_client_index'), 'back to client list') }}
            {% endif %}
            

		</div>
	</div>
{% endblock %}
