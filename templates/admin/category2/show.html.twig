<!DOCTYPE html>

<title>Category2</title>

{% block body %}
    <h1>Category2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category2.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ category2.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_category2_index') }}">back to list</a>

    <a href="{{ path('app_admin_category2_edit', {'id': category2.id}) }}">edit</a>

    {{ include('admin/category2/_delete_form.html.twig') }}
{% endblock %}
