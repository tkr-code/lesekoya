{% extends 'admin/base.html.twig' %}
{% import "macros/breadcrumb.html.twig" as utils %}

{% block title %}Edit OrderItem{% endblock %}

{% block breadcrumb %}
    {{ utils.breadcrumb('Edit Order',[
        {
            path:'order_index',
            name:'Manage orders'
        }
    ])}}
{% endblock %}

{% block body %}
<div class="row">
    <div class="col-6 mx-auto">
        <div class="card card-outline card-success">
            <div class="card-header">
                <h4>Edit Order Item</h4>
            </div>
            <div class="card-body">
            {{ include('admin/order_item/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
    </div>
</div>


    <a href="{{ path('order_edit',{id: order_item.commande.id,tab:'articles' },) }}">back to order</a>

    {{ include('admin/order_item/_delete_form.html.twig') }}
{% endblock %}
