<form method="post" action="{{ path('admin_article_buy_delete', {'id': article_buy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article_buy.id) }}">
    <button class="btn">Delete</button>
</form>
