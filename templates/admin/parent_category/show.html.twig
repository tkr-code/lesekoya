{% extends 'base.html.twig' %}

{% block title %}ParentCategory{% endblock %}

{% block body %}
    <h1>ParentCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parent_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ parent_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_parent_category_index') }}">back to list</a>

    <a href="{{ path('admin_parent_category_edit', {'id': parent_category.id}) }}">edit</a>

    {{ include('admin_parent_category/_delete_form.html.twig') }}
{% endblock %}
