{% extends "email/base.html.twig" %}
{% block body %}
<nav class="breadcrumb">
   <a class="breadcrumb-item" href="/">{{'Home'|trans}}</a>
   <span class="breadcrumb-item active">{{'Reset password'|trans}}</span>
</nav>
<h1>Hi!</h1>

<p>To reset your password, please visit the following link</p>

{# <a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a> #}

<p>This link will expire in 
{# {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}. #}
</p>

<p>Cheers!</p>
{% endblock %}
{% block btn_path %}{{ url('app_reset_password', {token: resetToken.token}) }} {% endblock %}
{% block btn_text %} {{ btn.text|trans}} {% endblock %}